library(readr)
library(dplyr)
library(RJSONIO)
rdi = read_tsv("./rdi.tsv")
rdi = read_tsv("./rdi.tsv")
rdi$Nutrient
rdi$PregnantWomen
rdi = read_tsv("./rdi.tsv")
json <- toJSON(rdi)
rdi$PregnantWomen
print(json)
json <- toJSON(rdi)
write.table(json, file = "rdi.json")
json <- toJSON(rdi)
json
write.table(json, file = "rdi.json", row.names = F, quote=F)
write.table(json, file = "rdi.json", row.names = F, quote=F, col.names = F)
